{
	"template" : "Update contract template",
	"api" : "${api}",
	"operation" : "${method}",
	"correlationId" : "${correlationId}",
	"customerExternalId" : "${custExtId}",
	"contractExternalId" : "${contExtId}"
}